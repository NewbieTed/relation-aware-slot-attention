[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scop-depth"
version = "0.1.0"
description = "SCOP-Depth: a depth-augmented extension of the CoMPaSS SCOP data pipeline"
readme = "scop_depth/README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.26.0",
  "pillow>=9.5.0",
  "requests>=2.31.0",
  "torch>=2.5.0",
  "transformers>=4.45.0",
  "tqdm>=4.66.0",
]

[project.optional-dependencies]
eval = [
  "accelerate>=0.34.0",
  "diffusers>=0.30.0",
  "safetensors>=0.4.5",
]
train = [
  "accelerate>=0.34.0",
  "diffusers>=0.30.0",
  "einops>=0.7.0",
  "safetensors>=0.4.5",
]
flux = [
  "accelerate>=0.34.0",
  "bitsandbytes>=0.43.3",
  "diffusers>=0.31.0",
  "einops>=0.7.0",
  "safetensors>=0.4.5",
]

[project.scripts]
scop-depth = "scop_depth.__main__:main"
scop-depth-train-graph = "training.pretrain_graph_encoder:main"
scop-depth-train-flux-lora = "training.train_relation_flux_lora:main"
scop-depth-generate-flux-relation = "evaluation.generate_flux_relation:main"

[tool.setuptools]

[tool.setuptools.packages.find]
include = ["scop_depth", "scop_depth.*", "evaluation", "evaluation.*", "training", "training.*"]
