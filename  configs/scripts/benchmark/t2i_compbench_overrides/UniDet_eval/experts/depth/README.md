# Depth Expert Override

This directory contains the patched T2I-CompBench depth helper for 3D spatial
evaluation.

## File

- `generate_dataset.py`: prepares generated images for the benchmark's depth
  expert.

This is benchmark compatibility code. The project’s own depth preprocessing
lives in `scop_depth/depth.py`.
